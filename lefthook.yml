pre-commit:
  commands:
    gofmt:
      run: gofmt -w cmd/ai-sandbox/main.go cmd/ai-sandbox/main_test.go
    fast-check:
      run: ./scripts/check-fast.sh

pre-push:
  commands:
    integration:
      run: make test-integration
